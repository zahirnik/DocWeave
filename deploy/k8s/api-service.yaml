apiVersion: v1
kind: Service
metadata:
  name: rag-api
  namespace: convai-rag
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: rag-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
