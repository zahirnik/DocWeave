apiVersion: v1
kind: Secret
metadata:
  name: rag-secrets
  namespace: convai-rag
type: Opaque
stringData:
  # Replace with real values or use an ExternalSecret/SealedSecret in production.
  OPENAI_API_KEY: ""
  TAVILY_API_KEY: ""
  DATABASE_URL: "postgresql+psycopg://rag:rag@postgres:5432/ragdb"
